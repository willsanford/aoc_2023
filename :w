use std::fs::read_to_string;

pub fn day23() {
    let filename = "data/day_1.txt";

    let ans: u32 = read_to_string(filename)
        .unwrap()
        .split('\n')
        .into_iter()
        .map(|line| {
            0
        })
        .sum();

    println!("{:?}", ans);
}
